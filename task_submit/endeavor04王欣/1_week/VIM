1.选择文本
  V（当前行开始，两次即可完成选择）
  v（当前位置，两次即可完成选择）

2.删除
  x(字符）
  dd(整行）
  J(换行符）

3.复制 粘贴 剪切
  yy(复制一行）
  p(粘贴在光标之前）
  P(粘贴在光标之后）大写
  d(剪切）

4.撤销与重做
  u(撤销）
  ctrl + r(重做）

5.保存与退出
  w(保存）
  q(退出）
  ZZ / wq / x (保存退出）
  ZQ / ql(不保存退出）
  w filename(另存为）
